version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MONGODB_URI=${MONGODB_URI}
      - PORT=8000

  frontend:
    build:
      context: ./client
    ports:
      - "3000:80"
    depends_on:
      - backend
